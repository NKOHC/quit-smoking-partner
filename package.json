{
  "name": "quit-smoking-partner",
  "version": "1.0.0",
  "description": "A web app to help quit smoking",
  "main": "index.js",
  "scripts": {
    "start": "http-server public",
    "build": "tsc && cp src/index.html public/ && cp -r src/assets public/",
    "dev": "npm run build && npm start",
    "watch": "tsc -w"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^18.0.0"
  }
}
